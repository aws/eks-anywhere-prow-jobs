jobName: tinkerbell-tooling-presubmit
runIfChanged: EKS_DISTRO_MINIMAL_BASE_TAG_FILE|EKS_DISTRO_MINIMAL_BASE_GLIBC_TAG_FILE|^build/lib/.*|Common.mk|projects/tinkerbell/tinkerbell/.*
commands:
- if $(make check-project-path-exists); then make build -C $PROJECT_PATH; fi
projectPath: projects/tinkerbell/tinkerbell
envVars:
- name: ARTIFACTS_BUCKET
  value: s3://projectbuildpipeline-857-pipelineoutputartifactsb-10ajmk30khe3f
- name: IMAGE_TAG_SUFFIX
  value: -amd64
imageBuild: true
resources:
  requests:
    memory: 16Gi
    cpu: 4
