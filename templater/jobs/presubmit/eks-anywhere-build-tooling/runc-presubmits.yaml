jobName: runc-tooling-presubmit
runIfChanged: build/lib/.*|Common.mk|projects/opencontainers/runc/.*
commands:
- if $(make check-project-path-exists); then make build -C $PROJECT_PATH; fi
projectPath: projects/opencontainers/runc
envVars:
- name: BINARY_PLATFORMS
  value: linux/amd64
- name: FAKE_ARM_ARTIFACTS_FOR_VALIDATION
  value: true
- name: ARTIFACTS_BUCKET
  value: s3://projectbuildpipeline-857-pipelineoutputartifactsb-10ajmk30khe3f
imageBuild: true
resources:
  requests:
    memory: 16Gi
    cpu: 4
