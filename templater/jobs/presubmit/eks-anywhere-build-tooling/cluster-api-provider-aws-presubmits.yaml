jobName: cluster-api-provider-aws-tooling-presubmit
runIfChanged: EKS_DISTRO_MINIMAL_BASE_TAG_FILE|^build/lib/.*|Common.mk|projects/kubernetes-sigs/cluster-api-provider-aws/.*
imageBuild: true
commands:
- if $(make check-project-path-exists); then make build -C $PROJECT_PATH; fi
projectPath: projects/kubernetes-sigs/cluster-api-provider-aws
resources:
  requests:
    memory: 16Gi
    cpu: 4
  limits:
    memory: 16Gi
    cpu: 4
